// import 'dart:typed_data';

// /// readU16 returns (value, newOffset)
// ({int item1, int item2}) readU16(Uint8List buf, int off) {
//   final v = (buf[off] << 8) | buf[off + 1];
//   return (item1: v, item2: off + 2);
// }

// /// readBytes returns (Uint8List, newOffset)
// ({Uint8List item1, int item2}) readBytes(Uint8List buf, int off, int len) {
//   return (item1: buf.sublist(off, off + len), item2: off + len);
// }
